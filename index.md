# SKTools

A collection of R functions that help with importing and analyzing data
frames.

**Install from GitHub**:

1.  If not installed already, install `remotes` package:
    `install.packages(“remotes”)`.
2.  Use `remotes::install_github(“samuelkaminsky/SKTools”)` to install
    the package.
