# Travis file
# See https://docs.travis-ci.com/user/languages/r/

language: r
cache: packages

r_packages:
  - covr

r_binary_packages: 
  - dplyr
  - tibble
  - stringr
  - broom
  - openxlsx
  - readxl
  - readr
  - tidyr
  - httr
  - jsonlite
  - effsize
  - crayon
  
apt_packages:
  - libgit2-dev

after_success:
  - Rscript -e 'library(covr); codecov()'
