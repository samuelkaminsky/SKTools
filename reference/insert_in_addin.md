# Insert %in% Operator in RStudio

Inserts %in% into text editor

## Usage

``` r
insert_in_addin()
```

## Examples

``` r
if (FALSE) { # \dontrun{
# This function is intended to be used as an RStudio Addin
insert_in_addin()
} # }
```
